# Toolchain check, 2026-09-23 13:06-13:12 PKT

Nine programs (list.txt): the demo and example projects and projectile.c
(cc1i), gcd, primes and rotmat (shci), smart.cpp and the cxx1i cases
class and lambda (cxx1i).

## Windows box - RIDE 4.0 freshly installed

Uninstalled, then RIDE-4.0-setup.exe (2026-09-22 23:26, 4,678,994 bytes)
installed silently; all eleven executables present. Chains per program,
each run and its output compared:

1. x86, RIDE's masm + RIDE's LINK (as RIDE sets *_AS and *_LD)
2. x86, RIDE's masm + Microsoft link.exe
3. x86, the vendor's: ml64 (cc1i, shci) or clang (cxx1i) + link.exe - no MASM, no LINK
4. C6000, each source to assembly and run on vm6747, as RIDE's Run does
5. C6000 linked, ASM6x + RIDE's LNK6x, and ASM6x + TI's lnk6x (cc1i, cxx1i)

    demo       cc1i   all four runs 3476824e, exit 0   LNK6x ok, TI ok
    example    cc1i   all four runs 994d95da           LNK6x ok, TI ok
    projectile cc1i   all four runs 4d109ffa           LNK6x ok, TI ok
    gcd        shci   masm+LINK SEGFAULT (139, no output); link.exe, vendor, vm6747 7cb028d9
    primes     shci   masm+LINK SEGFAULT;                   the other three b3db558c
    rotmat     shci   masm+LINK SEGFAULT;                   the other three de9b0d0e
    smart      cxx1i  all four runs f9d0b467           LNK6x ok, TI ok
    class      cxx1i  all four runs 22a39b5e = expected LNK6x ok, TI ok
    lambda     cxx1i  all four runs 6951866c = expected LNK6x ok, TI ok

**One inconsistency: shci's programs linked by RIDE's LINK crash.** The
same masm objects linked by Microsoft's link.exe run correctly, so it is
LINK, on what shci's objects and shmrt-x86_64-windows.lib ask of it. cc1i
and cxx1i agree on every chain. The C6000 images of one program differ by
4 bytes between runs, under both linkers: the objects are named after the
driver's pid in %TEMP%, and a pid of another length moves everything.

## Linux box - RIDE's toolchain as built there

No RIDE 4.0 build exists on the Linux box; ~/ride/bin is RIDE 3.5's,
built 2026-09-19 (cc1i, cxx1i, shci, asm6x, vm6747 - no lnk6x). A fresh
4.0 build is 11 minutes or more on the nano and did not fit. Chains: x86
through the native gcc/as/ld (no MASM or LINK exist there) and C6000 on
vm6747.

All nine agree, and every output is the Windows one byte for byte;
class and lambda match their recorded expectation.

## Harness faults met on the way, all mine

Git bash's coreutils `link` shadows link.exe (PATH now puts MSVC first);
macOS `._` files in the relay, which shci reads as sources of the module;
a runtime path with a space split by an unquoted substitution.
